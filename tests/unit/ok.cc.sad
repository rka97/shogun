#include "sg_gtest_utilities.h"
#include <iostream>

%include_subclasses_of(IterativeMachine)

using namespace shogun;

using IterativeMachines = Types<%subclasses_of(IterativeMachine)>;

template <typename T>
class IterativeMachinesTest : public ::testing::Test
{
};

SG_TYPED_TEST_CASE(IterativeMachinesTest, IterativeMachines);

TYPED_TEST(IterativeMachinesTest, Creation)
{
	auto machine = std::make_shared<TypeParam>();
	std::cout << machine->get_name() << '\n';
}
